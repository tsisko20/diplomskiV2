using UnityEngine;

namespace RTS.Enemy
{
    public class DefenseState : EnemyState
    {
        public DefenseState(EnemyContext _enemyContext, EnemyStateMachine _stateMachine) : base(_enemyContext, _stateMachine)
        {
        }
    }
}

